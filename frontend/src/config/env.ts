export const API_BASE_URL = "http://192.168.10.200:3000/api/v1";
// Note: on a physical device/emulator, replace 'localhost' with your machine's LAN IP,
// e.g. http://192.168.1.10:3000/api/v1 — we'll revisit this in the auth phase.
